# Citations guesser game

## To add changes

`git add .`
`git commit -m "message'`
`git push`

and then `npm run deploy -- -m "message"`

## TODO

- Add all audios
- Use a free web api
- Add authentification and profiles